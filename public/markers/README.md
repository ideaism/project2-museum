# Legacy Marker Folder

This folder is retained only for compatibility with an earlier AR marker prototype.

The current prototype uses camera gesture pouring, not AR marker tracking. Use `/gesture/:id` and `/camera-test` for assessment. Use `/object/:id` when camera access is unavailable.

No final assessment setup should depend on marker files in this folder.
